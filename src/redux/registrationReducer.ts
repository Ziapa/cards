
type InitialStateRegType = {
    loading: boolean
}


const initialState = {
loading: false
}


export const registrationReducer = (state: InitialStateRegType = initialState, action : any) : InitialStateRegType => {
    switch (action.type) {
        default:
            return state
    }
}

// ActionCreators



// THUNKCreators